# GitHub repo to clone into /workspace on container startup (public repos only)
GITHUB_REPO_URL=https://github.com/your-org/your-repo

# Root directory for file operations inside the container (default: /workspace)
WORKSPACE_DIR=/workspace

# Port the FastAPI server listens on (default: 8000)
API_PORT=8000

# URL the MCP server uses to reach the container (set in .mcp.json, not here)
# CONTAINER_BASE_URL=http://localhost:8000
